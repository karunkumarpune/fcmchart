<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firebasechat">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".chat.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".list_user.UserLists"
            android:theme="@style/AppTheme"
            />
        <activity android:name=".chat.RegistrationActivity"/>

        <activity android:name=".chat.Chat"
            android:theme="@style/AppTheme"
            />


    <!--    <activity android:name=".t.LoginActivity"/>
        <activity android:name=".t.RegistrationActivity"/>
        <activity android:name=".t.Chat"/>-->
    </application>

</manifest>